import 'package:flutter/material.dart';

class CustomTheme {
  const CustomTheme();

  static Color bgColor = Colors.grey.shade200;
  static const Color areaBgColor = Color.fromRGBO(60, 23, 93, 1);
}
